# 核心框架
express@^4.18.2
cors@^2.8.5
dotenv@^16.0.3

# 資料庫操作（MongoDB 或 PostgreSQL 二選一，看要用哪一個）
mongoose@^7.0.0        # 若用 MongoDB
# pg@^8.8.0             # 若用 PostgreSQL
# sequelize@^6.28.0     # 若用 Sequelize + PostgreSQL

# 驗證與安全性
bcrypt@^5.1.0
jsonwebtoken@^9.0.0
cookie-parser@^1.4.6

# 請求驗證與中介處理
express-validator@^6.14.3
morgan@^1.10.0

# 開發工具
nodemon@^2.0.22         # 僅開發用，不需部署

# 檔案上傳（如果未來有）
multer@^1.4.5-lts.1

# 時間處理（如活動時間）
dayjs@^1.11.7
